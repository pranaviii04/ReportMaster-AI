# Local default — api.js already falls back to http://127.0.0.1:8000
REACT_APP_API_URL=http://127.0.0.1:8000

# Production (set this in the Vercel dashboard, then rebuild):
# REACT_APP_API_URL=https://your-service.up.railway.app
